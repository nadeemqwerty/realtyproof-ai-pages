# RealtyProof AI Pages

This repository publishes a prebuilt static artifact from the private RealtyProof AI
source repository. It intentionally contains no application source, credentials, private
evidence, acquisition quarantine, or database configuration.

The deployed artifact is bound to `DEPLOYMENT-PROVENANCE.json`. Source changes are
reviewed privately before a validated static artifact is promoted here.
